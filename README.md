# Portfolio Starter (Static HTML/CSS/JS)

This is a simple, production-friendly starter for your personal portfolio. No frameworks required.

## ✨ Features
- Light/Dark theme toggle (saved in localStorage)
- Responsive layout (Hero, About, Services, Work, Contact)
- Accessible & semantic HTML
- Minimal, modern styling (glass + gradient highlight)
- Drop-in assets in `/assets`

## 🚀 Run
Open `index.html` in your browser, or use a simple local server:

**Option A – VS Code Live Server Extension**
1. Install the *Live Server* extension.
2. Right-click `index.html` → *Open with Live Server*.

**Option B – Python HTTP server**
```bash
# from this folder
python -m http.server 5500
# open http://localhost:5500
```

## 🖼 Replace assets
- `assets/profile-placeholder.png` – Your portrait
- `assets/project-*.png` – Project cover images

## 🧪 Customize
- Colors, spacing, typography are in `styles.css`.
- Sections live in `index.html`.
- Theme + mobile menu logic in `script.js`.

## 🌍 Deploy
- Drag & drop to Netlify / Vercel / GitHub Pages.
